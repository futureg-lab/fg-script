{
	num first = 1;
	// __mem_debug__();
	{
		num second = 2;
		// __mem_debug__();
		{
			num third = 3;
			// __mem_debug();
			println("Result is {0}", first + second + third);
		}
		// this should trigger an error
		println("Result is {0}", first + second + third);
	}
}

// __mem_debug__();